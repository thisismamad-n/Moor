name: web-openai-native
version: 1.0.0
description: "OpenAI native web search — declares the Responses API server-side ``web_search`` built-in instead of running a client-side search. Requires the Codex Responses transport plus openai-codex OAuth (``moor auth add openai-codex``)."
author: Moor inc.
kind: backend
provides_web_providers:
  - openai-native
