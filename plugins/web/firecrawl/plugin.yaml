name: web-firecrawl
version: 1.0.0
description: "Firecrawl web search + content extraction. Supports keyless cloud, direct API, and Moor-hosted tool-gateway routing for subscribers."
author: Moor inc.
kind: backend
provides_web_providers:
  - firecrawl
