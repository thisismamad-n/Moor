# Nous-approved MCP catalog entry.
# Presence in this directory = approval. Merged via PR review.
manifest_version: 1

name: atlassian
description: >-
  Jira issues and Confluence pages via Atlassian's hosted remote MCP.
source: https://support.atlassian.com/rovo/docs/getting-started-with-the-atlassian-remote-mcp-server/

# Official vendor-hosted remote MCP (URL-only — Moor never spawns a local
# process for this entry). Native OAuth 2.1 + Dynamic Client Registration;
# Moor's MCP client + mcp_oauth_manager handle discovery, PKCE, token
# exchange, and refresh.
transport:
  type: http
  url: https://mcp.atlassian.com/v1/sse

auth:
  type: oauth

# Composer-suggestion triggers (desktop brand pills).
suggest:
  keywords:
    - jira
    - confluence
    - atlassian
    - bitbucket
  hosts:
    - atlassian.net
    - atlassian.com
    - jira.com

post_install: |
  On first connection Moor opens a browser to authorize with
  Atlassian (or run `hermes mcp login atlassian`). Approve access,
  then restart the session so tools load.
